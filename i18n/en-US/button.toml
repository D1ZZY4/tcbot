# Button labels: every tap target in one place, never duplicated.
# Buttons render as plain text (Telegram never parses them), so values
# stay raw here; message paths escape them via placeholder rules.

back = "« Back"
cancel = "Cancel"
done = "Done"

# Display name of this locale, shown on language buttons.
language_name = "English (US)"
